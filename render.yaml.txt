services:
  - type: web
    name: wallstreet-fx-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn main:app --bind 0.0.0.0:$PORT --workers 1 --threads 2
    envVars:
      - key: FINNHUB_API_KEY
        sync: false
      - key: TELEGRAM_TOKEN
        sync: false
      - key: TELEGRAM_CHAT_ID
        sync: false
      - key: ACCOUNT_BALANCE
        value: "100"
      - key: RISK_PERCENT
        value: "2"
